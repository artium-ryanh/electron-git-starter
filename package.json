{
  "name": "my-electron-app",
  "version": "1.0.0",
  "description": "testing an Electron App",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && cp index.html dist && electron dist/main.js"
  },
  "author": "Ryan",
  "license": "ISC",
  "devDependencies": {
    "@types/electron": "^1.6.10",
    "electron": "^13.2.1",
    "typescript": "^4.3.5"
  }
}
